<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />

    <style>
        .nav-links {
            text-decoration: none;
            color: white;
            background-color: rgb(29, 78, 216);
            padding: 10px;
            border-radius: 5px;
            font-weight: 600;
            margin: 5px;
            /* width: 120px; */
            text-align: center;
            align-items: center;
            /* height: 60px; */
        }

        .textboxesLeft {
            background-color: rgba(153, 158, 163, 0.497);
            padding: 10px;
            width: auto;
            max-width: 360px;
            text-align: start;
            color: black;
        }

        .textboxesRight {
            background-color: rgba(161, 232, 203, 0.497);
            padding: 10px;
            width: auto;
            max-width: 360px;
            text-align: start;
            color: black;

        }

        .textboxesUnread {
            background-color: rgba(148, 46, 152, 0.497);
            padding: 10px;
            width: auto;
            max-width: 360px;
            text-align: start;
            color: black;

        }

        .modal {
            display: none;
            position: fixed;
            z-index: 1;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background-color: rgb(0, 0, 0);
            background-color: rgba(0, 0, 0, 0.4);
            padding-top: 60px;
        }

        .modal-content {
            background-color: #fefefe;
            margin: 5% auto;
            padding: 20px;
            border: 1px solid #888;
            width: 20%;
            height: auto;
        }

        .close {
            color: #aaa;
            float: right;
            font-size: 28px;
            font-weight: bold;
        }

        .close:hover,
        .close:focus {
            color: black;
            text-decoration: none;
            cursor: pointer;
        }
    </style>


</head>

<body class="mx-auto max-w-[1280px]">


    <nav>
        <div class="flex justify-between p-5 bg-blue-700 text-white">
            <div class="flex justify-start ">
                <a href="/" class="text-white font-bold text-xl p-3">COMP 3920 Assignment 2</a>
            </div>
            <div class="flex justify-end ">


                <% for (let i=0; i < navLinks.length; i++) { %>
                    <a href="<%= navLinks[i].link %>"
                        class="nav-links  <% if (navLinks[i].link == currentURL) { %> bg-orange-400<% } %> hover:bg-blue-800">
                        <%= navLinks[i].text %>
                    </a>
                    <% } %>



            </div>
        </div>
    </nav>

    <div class="flex justify-center items-center p-5 text-blue-700 ">
        <h1 class="text-5xl self-center">Abhi's Assignment 2 for COMP 3920
        </h1>
    </div>